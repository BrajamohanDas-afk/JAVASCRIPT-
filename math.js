// Math = built-in object that provides a
//        collection of properties and methods

// console.log(Math.PI);
// console.log(Math.E);



let x=3;
let y=2;
let z=1;

// z=Math.round(x);
// z=Math.floor(x);   //it rounds down
// z=Math.ceil(x);   //it rounds up
// z=Math.trunc(x);   //will wlwment any decimal no.
// z=Math.pow(x,y);   //power
// z=Math.sqrt(x);   //squre root
// z=Math.log(x);   //log
// z=Math.sin(x);
// z=Math.cos(x);
// z=Math.tan(x);
// z=Math.abs(x);  //make the no. positive

// let max=Math.max(x,y,z);
let min=Math.min(x,y,z);



console.log(min)